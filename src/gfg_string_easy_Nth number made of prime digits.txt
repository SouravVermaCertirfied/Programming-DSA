// { Driver Code Starts
//Initial function template for C++

#include<bits/stdc++.h>
using namespace std;

 // } Driver Code Ends
//User function template for C++

class Solution
{
    public:
    //Function to find nth number made of only prime digits.
	bool lastNumberIsValid(int lastdigit){
		bool ans = false;
		if(lastdigit == 2 || lastdigit == 3 ||lastdigit == 5 ||lastdigit == 7 ){
			ans = true;
		}
		else
			ans = false;
		return ans;
	}

    bool eachNumberIsPrime(int num){
        int lastdigit = 0;
        while(num != 0){
            lastdigit = num%10;
            num = num/10;
            if(lastNumberIsValid(lastdigit)){
                    /*do nothing*/
                }
            else{
            	return false;
            }
        }
        return true;
    }
    int primeDigits(int n)
    {
        //code here
        int num=1;
        int count = 0;
        while(count<n){
            if(eachNumberIsPrime(num)){
                count++;
            }
            num++;
        }
        return --num;
    }
};

// { Driver Code Starts.
int main()
{
    int t;
//    cin>>t;
    t = 1;
    // All the above tasks are preprocessing, now you can run testcases
    // and then print the values accordingly
    while(t--)
    {
        int n;
//        cin>>n;
        n = 10;
        Solution ob;
        cout << ob.primeDigits(n) << "\n";
    }
}
  // } Driver Code Ends
