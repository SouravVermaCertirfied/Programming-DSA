- while passing head to a a function, say addNode in a linked list.
following is how it should be done:
function call : 
Node *head = NULL;
addNode(&head, val;)

function definatoin:
void addNode( Node **head, int val) // notice the double pointer
*head = NULL  ;  // updating value of NULL within the function
if( *head == NULL) // comparing the head within the function
Node *temp = *head // reading the value of head